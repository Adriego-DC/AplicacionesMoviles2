<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.actividades">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Actividades"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de las otras actividades -->
        <activity
            android:name=".MainActivityPrimo"
            android:exported="true" />
        <activity
            android:name=".MainActivityEdades"
            android:exported="true" />
        <activity
            android:name=".MainActivityArea"
            android:exported="true" />
        <activity
            android:name=".MainActivityTrabajadores"
            android:exported="true" />
        <activity
            android:name=".MainActivityFactores"
            android:exported="true" />
        <activity
            android:name=".MainActivityCompuesto"
            android:exported="true" />

    </application>

</manifest>